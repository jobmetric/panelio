<div class="card h-lg-100">
    <a href="{{ $link }}" class="card-body w-100 text-center">
        {{ $slot }}
        <span class="fw-semibold fs-5 pt-5 text-gray-400 d-block">{{ $name }}</span>
    </a>
</div>
